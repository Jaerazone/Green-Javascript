변수 선언할때
var 이름이 겹치거나 수정하게되면 오류발생할수있음
var 자체가 전체를 다 포용하기때문에
let으로 변수 지정해주는게 좋다

> let 식별자;
> let pi; // 선언
> pi=3.14; //할당

혹은
> let pi=3.14; //한꺼번에 다넣는방법
	     // 선언하면서 값주는걸 '초기화'라고함

변수선언할때
1) 특수문자사용안되지만 & _ 두개는 가능
2) 숫자안됨
3) 대소문자 구분한다